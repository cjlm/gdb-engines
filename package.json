{
  "name": "gdb-engines",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "favicons": "node scripts/extract-favicons.mjs",
    "graphs": "node scripts/generate-graphs.mjs",
    "check-evidence": "node scripts/check-evidence.mjs",
    "verify-quotes": "node scripts/verify-quotes.mjs",
    "predev": "npm run favicons && npm run graphs",
    "dev": "astro dev",
    "prebuild": "npm run favicons && npm run graphs && npm run check-evidence",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@11ty/eleventy-fetch": "^5.1.1",
    "@astrojs/sitemap": "^3.7.1",
    "astro": "^5.17.1",
    "astro-og-canvas": "^0.11.1",
    "canvaskit-wasm": "^0.41.1",
    "garphield": "0.1.1",
    "smol-toml": "1.6.0"
  }
}
