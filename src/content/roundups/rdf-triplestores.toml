slug = "rdf-triplestores"
title = "RDF Triplestores Compared"
h1 = "RDF triplestores compared"
lede = "Databases built on the RDF data model and queried with SPARQL."

# All conditions AND together; list values are OR. Members are always active-only;
# filtered-out inactive members are listed in the page footnote instead.
[filter]
type = ["RDF"]

include = []
exclude = []
ranking_board = "rdf"
