slug = "java-graph-databases"
title = "Java Graph Databases Compared"
h1 = "Java graph databases compared"
lede = "Graph databases implemented in Java."

# All conditions AND together; list values are OR. Members are always active-only;
# filtered-out inactive members are listed in the page footnote instead.
[filter]
implementation_language = ["Java"]

include = []
exclude = []
ranking_board = "java"
