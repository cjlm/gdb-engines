# Sources for values in src/content/databases/terminusdb.toml.
# Written by scripts/import-research.mjs and scripts/verify-quotes.mjs — edit values in the
# catalogue entry, not here; scripts/check-evidence.mjs fails the build if the two disagree.

slug = "terminusdb"

[[claims]]
field = "protocols"
value = ["HTTP/REST", "GraphQL (HTTP)"]
confidence = "high"
method = "repo"
checked = "2026-07-31"
notes = "Document/REST API and GraphQL are both served over HTTP on the default server port (6363); WOQL is a query language layered on the same HTTP API, not a separate wire protocol."

  [[claims.sources]]
  url = "https://github.com/terminusdb/terminusdb/blob/main/README.md"
  title = "README.md · terminusdb/terminusdb"
  quote = "Use REST API as a proper graph query language with deep link discovery, path queries and linked data"
  verified = "matched"
  checked = "2026-08-01"

  [[claims.sources]]
  url = "https://github.com/terminusdb/terminusdb/blob/main/README.md"
  title = "README.md · terminusdb/terminusdb"
  quote = "Use GraphQL as a proper graph query language with deep link discovery and path queries"
  verified = "matched"
  checked = "2026-08-01"
