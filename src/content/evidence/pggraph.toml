# Sources for values in src/content/databases/pggraph.toml.
# Written by scripts/import-research.mjs and scripts/verify-quotes.mjs — edit values in the
# catalogue entry, not here; scripts/check-evidence.mjs fails the build if the two disagree.

slug = "pggraph"

[[claims]]
field = "protocols"
value = ["PostgreSQL wire"]
confidence = "high"
method = "repo"
checked = "2026-07-31"
notes = "PostgreSQL extension; queried entirely through SQL functions in the graph schema, no separate network protocol."

  [[claims.sources]]
  url = "https://github.com/Evokoa/pgGraph"
  title = "Evokoa/pgGraph"
  quote = "pgGraph is a PostgreSQL extension for running graph search, traversal, shortest path, and relationship queries directly against ordinary PostgreSQL tables."
  verified = "matched"
  checked = "2026-08-01"
